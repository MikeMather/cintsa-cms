import styled from 'styled-components';

const AppContainer = styled.div`
    width: 95%;
    height: 95%;
    background: white;
    overflow-y: auto;
    pointer-events: all;
`;

export default AppContainer;
