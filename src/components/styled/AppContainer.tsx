import styled from 'styled-components';

const AppContainer = styled.div`
    width: 90%;
    height: 90%;
    background: white;
    overflow-y: auto;
    background: white;
    pointer-events: all;
`;

export default AppContainer;
